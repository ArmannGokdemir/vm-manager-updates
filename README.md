# vm-manager-updates

VM Manager otomatik güncelleme feed'i. Uygulama açılışta `latest.json`'u okur;
daha yeni sürüm varsa "İndir" bildirimi gösterir.
(Kaynak kod ve exe burada DEĞİL — sadece sürüm bilgisi.)
